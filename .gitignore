# macOS
.DS_Store
.AppleDouble
.LSOverride
Icon
._*
.DocumentRevisions-V100
.fseventsd
.Spotlight-V100
.TemporaryItems
.Trashes
.VolumeIcon.icns
.com.apple.timemachine.donotpresent
.AppleDB
.AppleDesktop
Network Trash Folder
Temporary Items
.apdisk

# Linux
*~
.fuse_hidden*
.directory
.Trash-*
.nfs*


# Python
__pycache__/
*.pyc
.venv/
venv/
*.egg-info/

# build output
mapping_layer/build/dist/
mapping_layer/build/*.spec

# Faust: nella cartella receivers/faust tieni solo i sorgenti .dsp e il
# README -- ignora tutto il resto (eseguibili compilati senza estensione,
# .cpp intermedi, .o, bundle .app/.dSYM, qualunque nome assumano)
mapping_layer/receivers/faust/*
!mapping_layer/receivers/faust/*.dsp
!mapping_layer/receivers/faust/*.md