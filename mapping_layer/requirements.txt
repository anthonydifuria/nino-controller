pyserial
python-osc
mido

# macOS note: if you have JACK installed (e.g. via Homebrew), installing
# python-rtmidi from this file can fail to compile (a known RtMidi bug
# with the JACK backend on Darwin). If that happens, see the
# "Troubleshooting" section in this folder's README.
python-rtmidi
